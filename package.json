{
  "name": "crowdmint",
  "license": "MIT",
  "author": "niklr",
  "homepage": "https://github.com/niklr",
  "version": "0.0.1",
  "scripts": {
    "install-contracts": "yarn install --cwd contracts",
    "install-client": "yarn install --cwd client",
    "install-workers": "yarn install --cwd workers",
    "install-all": "yarn install-contracts && yarn install-client && yarn install-workers"
  }
}